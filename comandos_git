git add  nome_arquivo ou -A ou *
git init
git commit -m "mensagem"
git status
git config user.name "username"
git config --global user.name "user name"
git config --global user.email "email"
git branch xxxxxx
git 

- ALTEIREI O ARQUIVO
- usei o comando git add comandos_git para adicionar
- usei o comando git commit para salvar o arquivo
- modifiquei e salvei o arquivo no github no repositório que tinha

git init
git branch
git config
git checkout